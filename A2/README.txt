A text based java program detailing song statistics such as lyrics, artist, and more 

To run the program make sure folder directory is correct then type: "javac MyAudioUI.java" into terminal for the first run so the class files are created. 
Then, "java MyAudioUI.java" in terminal for subsequent runs to start the program. 
